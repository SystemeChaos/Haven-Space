import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.systemechaos.havenspace',
  appName: 'Haven Space',
  webDir: 'dist'
};

export default config;
